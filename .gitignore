# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Ignore bin directory from builds
bin/

# Ignore kubebuilder test binaries
testbin/

# Webhook service is copied from config/webhook/ by "make bundle"
# while it's not needed as it's generated by OLM
bundle/manifests/webhook-service_v1_service.yaml
